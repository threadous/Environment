local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RbxEnv = require(ReplicatedStorage.RbxEnv.Env)

RbxEnv.LoadClient() -- Add this code only to the client after requiring the module
